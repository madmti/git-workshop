---
title: "Reiniciar Desmitificado"
description: "Los tres arboles de Git (HEAD, indice y directorio de trabajo) y como reset los manipula con --soft, --mixed y --hard, incluyendo reset con rutas, squash con reset y las diferencias con checkout."
orden: 12
label: "CLASE 12"
duracion: "30m"
dificultad: dificil
recursos:
  - titulo: "Pro Git — Cap. 7.7: Reset Demystified"
    url: https://git-scm.com/book/en/v2/Git-Tools-Reset-Demystified
draft: false
---

import FullTitle from 'MDX/FullTitle.astro';
import Image from 'MDX/Image.astro';
import ImageSet from 'MDX/ImageSet.astro';

<FullTitle>
    <span class="flex flex-col items-center">
        <p>Herramientas de **Git**</p>
        <small class="text-lg mt-2">Reiniciar Desmitificado</small>
    </span>
</FullTitle>

---

## Los Tres Arboles

Una manera facil de entender `reset` y `checkout` es pensar en _Git_ como un administrador de contenido de **tres arboles diferentes**.
Por "arbol" nos referimos a "coleccion de archivos", no necesariamente a la estructura de datos.

| Arbol                 | Rol                                               |
|-----------------------|---------------------------------------------------|
| HEAD                  | Ultima instantanea del commit, proximo padre      |
| Indice                | Siguiente instantanea del commit propuesta        |
| Directorio de Trabajo | Caja de arena                                     |

---

### HEAD

HEAD es el puntero a la referencia de la rama actual, que a su vez apunta al ultimo commit realizado en esa rama. HEAD sera el padre del proximo commit que se cree. En general, es mas simple pensarlo como la instantanea de **tu ultimo commit**.

```bash
git cat-file -p HEAD
git ls-tree -r HEAD
```

### El Indice

El indice es tu **siguiente commit propuesto**, tambien conocido como el _"Area de Preparacion"_. _Git_ lo rellena con una lista de todos los contenidos del archivo revisados por ultima vez en tu directorio de trabajo.

```bash
git ls-files -s
```

### El Directorio de Trabajo

Los otros dos arboles almacenan su contenido de manera eficiente pero inconveniente dentro de la carpeta `.git`. El directorio de trabajo los descomprime en archivos reales, haciendo mucho mas facil editarlos. Piensalo como una **caja de arena** donde puedes probar cambios antes de enviarlos al indice y luego al historial.

---

## El Flujo de Trabajo

El objetivo principal de _Git_ es registrar instantaneas de tu proyecto en estados sucesivamente mejores, mediante la manipulacion de estos tres arboles.

<Image
    url="/git-workshop/08/reset-workflow.png"
    alt="Flujo de trabajo de los tres arboles"
    fit="contain"
    aspect="wide"
    tw="max-w-2xl mx-auto"
/>

---

Visualicemos el proceso con un ejemplo. Supongamos que entras a un directorio nuevo con un solo archivo (**v1**). Ejecutas `git init`, lo que crea un repositorio con HEAD apuntando a una rama aun no nacida.

<Image
    url="/git-workshop/08/reset-ex1.png"
    alt="Estado inicial: solo el directorio de trabajo tiene contenido"
    fit="contain"
    aspect="wide"
    tw="max-w-2xl mx-auto"
/>

---

Ejecutas `git add` para copiar el archivo al indice:

<Image
    url="/git-workshop/08/reset-ex2.png"
    alt="git add copia el archivo al indice"
    fit="contain"
    aspect="wide"
    tw="max-w-2xl mx-auto"
/>

---

Luego `git commit`, que guarda el indice como una instantanea permanente y actualiza HEAD:

<Image
    url="/git-workshop/08/reset-ex3.png"
    alt="git commit guarda la instantanea y actualiza HEAD"
    fit="contain"
    aspect="wide"
    tw="max-w-2xl mx-auto"
/>

---

Ahora modificas el archivo (**v2**) y repites el proceso:

<ImageSet
    layout="flex"
    containerTw="my-4 justify-center"
    imageTw="max-w-sm"
    fit="contain"
    aspect="wide"
    tw="max-w-2xl mx-auto"
    images={[
        { alt: 'v2 en el directorio de trabajo', url: '/git-workshop/08/reset-ex4.png' },
        { alt: 'git add copia v2 al indice', url: '/git-workshop/08/reset-ex5.png' },
        { alt: 'git commit guarda v2', url: '/git-workshop/08/reset-ex6.png' },
    ]}
/>

---

## El Papel del Reinicio

Supongamos que confirmamos el archivo por tercera vez (**v3**). Nuestro historial luce asi:

<Image
    url="/git-workshop/08/reset-start.png"
    alt="Historial con tres commits"
    fit="contain"
    aspect="wide"
    tw="max-w-2xl mx-auto"
/>

`reset` manipula los tres arboles de forma simple y predecible, realizando hasta tres operaciones en orden.

---

### Paso 1: Mover HEAD (`--soft`)

Lo primero que hace `reset` es mover la rama a la que apunta HEAD. Si HEAD esta en `master`, ejecutar `git reset HEAD~` hara que `master` apunte al commit anterior.

<Image
    url="/git-workshop/08/reset-soft.png"
    alt="reset --soft mueve HEAD al commit anterior"
    fit="contain"
    aspect="wide"
    tw="max-w-2xl mx-auto"
/>

Con `--soft`, el comando se detiene aqui. Esto deshace el ultimo `git commit` sin tocar el indice ni el directorio de trabajo. Es equivalente a deshacer el commit y dejar todo preparado para volver a confirmar.

---

### Paso 2: Actualizar el Indice (`--mixed`)

El siguiente paso es actualizar el indice con el contenido de la instantanea a la que ahora apunta HEAD.

<Image
    url="/git-workshop/08/reset-mixed.png"
    alt="reset --mixed actualiza el indice"
    fit="contain"
    aspect="wide"
    tw="max-w-2xl mx-auto"
/>

Con `--mixed` (que es el **comportamiento por defecto**), el comando se detiene aqui. Esto deshace el ultimo commit **y** hace _unstage_ de todo — retrocede a antes de ejecutar `git add` y `git commit`.

---

### Paso 3: Actualizar el Directorio de Trabajo (`--hard`)

El tercer paso hace que el directorio de trabajo se parezca al indice.

<Image
    url="/git-workshop/08/reset-hard.png"
    alt="reset --hard actualiza tambien el directorio de trabajo"
    fit="contain"
    aspect="wide"
    tw="max-w-2xl mx-auto"
/>

Con `--hard`, el comando completa las tres operaciones. Deshace el ultimo commit, el `git add`, y **todo el trabajo en el directorio de trabajo**.

> `--hard` es la unica forma de hacer que `reset` sea peligroso. Git destruira los datos del directorio de trabajo sin posibilidad facil de recuperacion. Usalo con cuidado.

---

### Resumen de los tres modos

`reset` sobrescribe los tres arboles en orden especifico, deteniendose cuando se le indica:

- Mueve HEAD de la rama -> *(se detiene aqui si `--soft`)*
- Actualiza el indice para que se vea como HEAD -> *(se detiene aqui si `--mixed` o sin opcion)*
- Actualiza el directorio de trabajo para que se vea como el indice -> *(solo si `--hard`)*

---

## Reset con una Ruta

Si especificas una ruta de archivo, `reset` omite el paso 1 (no puede mover HEAD a solo parte de un commit) y limita su accion a ese archivo especifico.

<br/>

Por ejemplo, `git restore --staged file.txt` es equivalente a `git reset HEAD file.txt`, que copia `file.txt` de HEAD al indice, haciendo _unstage_ del archivo:

<Image
    url="/git-workshop/08/reset-path1.png"
    alt="reset con ruta hace unstage del archivo"
    fit="contain"
    aspect="wide"
    tw="max-w-2xl mx-auto"
/>

---

Tambien puedes especificar un commit concreto para extraer una version anterior de un archivo directamente al indice:

```bash
git reset eb43bf file.txt
```

<Image
    url="/git-workshop/08/reset-path3.png"
    alt="reset con commit y ruta extrae una version anterior del archivo"
    fit="contain"
    aspect="wide"
    tw="max-w-2xl mx-auto"
/>

---

## Squash con Reset

Una tecnica util con `reset --soft` es aplastar (_squash_) varios commits en uno. Si tienes commits intermedios de trabajo en progreso:

<Image
    url="/git-workshop/08/reset-squash-r1.png"
    alt="Historial con commits de trabajo en progreso"
    fit="contain"
    aspect="wide"
    tw="max-w-2xl mx-auto"
/>

---

Puedes ejecutar `git reset --soft HEAD~2` para mover HEAD dos commits atras, dejando el indice intacto:

<Image
    url="/git-workshop/08/reset-squash-r2.png"
    alt="reset --soft mueve HEAD dos commits atras"
    fit="contain"
    aspect="wide"
    tw="max-w-2xl mx-auto"
/>

---

Y luego hacer un nuevo `git commit` que los agrupa a todos en uno:

<Image
    url="/git-workshop/08/reset-squash-r3.png"
    alt="El nuevo commit agrupa el trabajo en progreso"
    fit="contain"
    aspect="wide"
    tw="max-w-2xl mx-auto"
/>

---

## Reset vs Checkout

`checkout` tambien manipula los tres arboles, pero con diferencias importantes respecto a `reset`:

<br />

**Sin rutas:** `git checkout <rama>` es similar a `git reset --hard <rama>`, pero con dos diferencias clave:
- `checkout` es **seguro para el directorio de trabajo**: intenta una fusion trivial antes de sobrescribir, no destruye cambios sin avisar.
- `checkout` mueve **HEAD** a otra rama; `reset` mueve la rama a la que HEAD apunta.

<Image
    url="/git-workshop/08/reset-checkout.png"
    alt="Diferencia entre checkout y reset respecto a HEAD"
    fit="contain"
    aspect="wide"
    tw="max-w-2xl mx-auto"
/>

**Con rutas:** `git checkout <commit> <archivo>` actualiza el indice **y** el directorio de trabajo con esa version del archivo. No mueve HEAD, pero **no es seguro** para el directorio de trabajo porque sobrescribe el archivo sin avisar.

---

## Tabla Resumen

| Comando                      | HEAD | Indice | Dir. Trabajo | Seguro? |
|------------------------------|------|--------|--------------|---------|
| `reset --soft [commit]`      | REF  | NO     | NO           | SI      |
| `reset [commit]`             | REF  | SI     | NO           | SI      |
| `reset --hard [commit]`      | REF  | SI     | SI           | **NO**  |
| `checkout [commit]`          | HEAD | SI     | SI           | SI      |
| `reset (commit) [file]`      | NO   | SI     | NO           | SI      |
| `checkout (commit) [file]`   | NO   | SI     | SI           | **NO**  |

<br />

> La columna HEAD dice "REF" si el comando mueve la rama a la que HEAD apunta, y "HEAD" si mueve el propio HEAD. Presta atencion especial a la columna "Seguro": si dice NO, piensalo bien antes de ejecutar.

---

## Recursos

- [Pro Git - Cap. 7.7: Reset Demystified](https://git-scm.com/book/en/v2/Git-Tools-Reset-Demystified): `7.7` — los tres arboles, reset y checkout.
